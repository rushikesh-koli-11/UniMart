version: "3.9"

services:
  backend:
    build: ./backend
    container_name: unimart-backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    restart: always

  frontend:
    build: ./frontend
    container_name: unimart-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
